@model client.Models.KategoriModel
@{
    ViewBag.Title = "Tambah Kategori";
}

<h2>Tambah Kategori Baru</h2>

@using (Html.BeginForm())
{
    <div class="form-horizontal">
        <div class="form-group">
            <label class="control-label col-md-2">Nama Kategori</label>
            <div class="col-md-10">
                @Html.TextBoxFor(m => m.nama_kategori, new { @class = "form-control" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Simpan" class="btn btn-success" />
                <a href="@Url.Action("Index")" class="btn btn-default">Kembali</a>
            </div>
        </div>
    </div>
}
